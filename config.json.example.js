// jshint esversion: 5
module.exports = {
    // acc name (main, 200 score)
    "token": "your token here",

    // acc name (0 scores, needs phone verification)
    // "token": "",

    // acc name (2 scores)
    // "token": "",

    "debugMode": true  // leave this on for some reason i dont remember
};
